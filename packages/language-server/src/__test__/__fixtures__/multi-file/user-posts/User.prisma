/// This is the user of the platform
model User {
    id    String @id @default(uuid()) @map("_id")
    name  String
    email String
    posts Post[]

    address Address

    favouriteAnimal FavouriteAnimal
}
